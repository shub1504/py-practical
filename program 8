#write a program to create a list of the cubes of only the even integers appearing in the input list using for loop
#the data in the list can be vary
def cube_even_integers(input_list):
    result = []
    for num in input_list:
        if num % 2 == 0:  # Check if the number is even
            result.append(num ** 3)
        else:
            continue

    return result


input_list = [4,5,6,7,8,9]

output_list = cube_even_integers(input_list)
print("Cubes of even integers:", output_list)


'''output
Cubes of even integers: [64, 216, 512]
'''
